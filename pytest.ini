[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
markers =
    smoke: critical happy-path tests for core functionality
    regression: broader validation, edge cases, and business-rule tests